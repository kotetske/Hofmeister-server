const {AccountType, Bank, Currency} = require("../models/models")
const ApiError = require('../error/ApiError')

class AccountMetaController {
    async getAll(req, res) {

    }
}

module.exports = new AccountMetaController()